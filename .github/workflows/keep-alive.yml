name: Keep Render Awake
on:
  schedule:
    - cron: "*/6 * * * *" 
  workflow_dispatch:
    
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: curl -s https://laundrybooking.onrender.com > /dev/null